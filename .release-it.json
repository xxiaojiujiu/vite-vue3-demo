{
    "git": {
        "commitMessage": "release: update version to ${version}",
        "tagAnnotation": "${version}"
    },
    "hooks": {
        "before:git:init": "git tag -a ${version} -m ${version} & npm run changelog & git tag -d ${version}"
    }
}