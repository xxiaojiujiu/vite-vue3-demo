{
    "git": {
        "commitMessage": "release: update version to ${version}",
        "tagAnnotation": "${version}"
    },
    "hooks": {
        "before:git:commit": "git tag -a ${version} -m ${version} & npm run changelog & git tag -d ${version}"
    }
}